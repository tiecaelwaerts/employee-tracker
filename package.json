{
  "name": "employee-tracker",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "license": "MIT",
  "description": "A command-line application that allows the user to manage a company's employees using node, inquirer, and postgres.",
  "keywords": [
    "database",
    "node"
  ],
  "dependencies": {
    "console.table": "^0.10.0",
    "dotenv": "^16.4.7",
    "inquirer": "^12.4.1",
    "pg": "^8.13.3",
    "postgres": "^3.4.5"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
